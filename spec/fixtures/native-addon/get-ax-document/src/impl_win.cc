#include "impl.h"

namespace impl {

std::string GetAccessibilityDocument(char* handle, size_t size) {
  // AXDocument is a macOS-specific accessibility attribute
  return "";
}

}  // namespace impl
